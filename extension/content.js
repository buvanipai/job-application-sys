// Optional content script. The popup uses chrome.scripting.executeScript to scrape
// the active tab on demand, so this file is intentionally minimal — kept as a
// placeholder for future features (auto-detect JD boards, highlight matching
// skills inline, etc).

console.log("[Talentpath] content script loaded on", location.href);
